apiVersion: v1
kind: Service
metadata:
  name: cpulse-api
  namespace: conversion-pulse
spec:
  type: ClusterIP
  selector:
    app: cpulse-api
  ports:
    - name: http
      port: 80
      targetPort: 3000
